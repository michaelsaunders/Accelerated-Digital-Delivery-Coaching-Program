@model IEnumerable<Accelerated_Digital_Delivery_Coaching_Program.Models.ProductBacklog>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>


<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductBacklogName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductDefintionOfDone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductBacklogName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductDefintionOfDone)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProductBacklogID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProductBacklogID">Show Backlog</a> |
                <a asp-action="Delete" asp-route-id="@item.ProductBacklogID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
