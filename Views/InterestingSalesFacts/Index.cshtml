@model IEnumerable<Accelerated_Digital_Delivery_Coaching_Program.Models.InterestingSalesFacts>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fact)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fact)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InterestingSalesFactsID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InterestingSalesFactsID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InterestingSalesFactsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
